# 员工离职挽留｜Agent 使用须知

本须知适用于所有能读取 `SKILL.md`、处理用户材料、执行本地 Python 脚本的 Agent，不依赖单一平台。平台侧元数据只负责发现与触发，核心流程以根目录 `SKILL.md` 和本文件为准。

## 什么时候使用

- 用户说某位核心成员状态不对、可能要走、想离职、要留人、离职挽留。
- 用户说某人已表达想走、已拿到别家 offer、已递辞呈，问怎么谈、要不要挽留。
- 用户要准备一次加薪、晋升、股权或认可的沟通。
- 团队敬业度调研暴露出某群体信号偏弱，需要落到对具体成员的一对一跟进。

不使用、明确拒绝：离职预测打分、人员排序、对不归自己带的人做背对背画像、在员工不知情时做监控。

## 两段式工作方式（核心）

**第一段：先给标准挽留方案。** 不管信息多少，先输出一套覆盖五种离职状态的标准方案，让管理者立刻有可对照、可动手的东西。

**第二段：追问核心原因，再个性化。** 在标准方案末尾，一定要问：「你认为这位员工要走的核心原因是什么？」拿到回答后，据此生成针对性的干预、谈话脚本和核实问题。

不要跳过第一段直接问，也不要给完标准方案就停下——两段都要有。

## 信息来源：用户输入 + 飞书证据

- **用户直接提供的信息**：管理者在对话里描述的情况、观察、该成员近期表现。默认基于这些做诊断。
- **飞书上已存在的证据**：如果用户指明了是哪位成员、并授权，读取近期 1:1 记录、会议纪要、OKR、和该成员的交流内容等真实证据来支撑建议。读取只读、脱敏、限于管理者自己的直属成员，见 `references/feishu-integration.md`。没有飞书证据时，只用用户提供的信息，一样能完成。

## 执行顺序

### 1. 确认场景与边界
谁在用（管理者对自己成员，还是第三方）、处在什么阶段（信号微弱/已表达/已有 offer/已递辞呈）、根因是否在可改变范围内。要对不归自己带的人做判断时，停止并说明边界。

### 2. 收集信息
先用用户提供的信息；用户指明成员并授权时，从飞书只读梳理该成员的信号与成长轨迹。以飞书为例先核验身份：

```bash
lark-cli auth status --json --verify
```

把两类来源整理成脱敏信号 JSON（见 `references/input-schema.md`）。

### 3. 出标准方案 + 追问
```bash
python3 scripts/diagnose.py --input <signals.json> --format markdown
```
输出会先给标准挽留方案，再在末尾问核心原因。把这个问题原样抛给用户。

### 4. 个性化
拿到用户判断的核心原因后：
```bash
python3 scripts/diagnose.py --input <signals.json> --reason "<用户的原因>" --format markdown
```
原因可以是五状态之一，也可以是自由文（如「晋升没希望」「翻倍 offer」「家在外地」）。脚本会映射到最贴合的状态并个性化；映射不到的（搬家、换赛道等），重点转向体面告别。

### 5. 准备谈话
按 `references/conversation-scripts.md` 准备。已有 offer 时按 `references/has-another-offer.md` 的顺序，别先问「要什么才留下」。

## 停止规则

- 用户要监控、预测打分、人员排序或背对背画像时，停止并说明边界。
- 信号不足时如实说「证据不足」，只给要核实的问题，不硬下结论。
- 读外部系统前先确认身份与授权，只读、脱敏。
- 有些人留不住是正常的。好聚好散，别把挽留话术强加给已决定离开的人。

## 边界一句话

保留的前提是对方相信你因为在意他而行动，而不是怕失去他。这枚 Skill 帮管理者更好地在意人，不做监控和打分。
